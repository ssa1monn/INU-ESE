/*
    <ctype.h>
        isalpha, isupper, islower, isdigit, isalnum, isspace
        toupper, tolower, toascii
    
    <string.h>
        strlen, strcpy(여기에, 이걸), strcat(여기에, 이걸), strcmp (같으면 0 반환), strchr(어디에서, 이걸), strstr
        strtok(문자열, 구분할 문자들) 계속해서 찾을때는 srtrok(NULL, 구분할 문자들)
        주소값을 반환함.
    
    <stdlib.h>
        atoi, atof
*/
